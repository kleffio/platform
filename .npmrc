# Hoist @types/* so TypeScript can find them via standard node_modules traversal.
# Hoist @kleff/* so Tailwind's @source scanner can find workspace packages.
hoist-pattern[]=@types/*
hoist-pattern[]=@kleff/*
