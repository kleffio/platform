# @kleff/shared-types

TypeScript types shared across all Kleff apps and services.

## Usage

```ts
import type { User } from "@kleff/shared-types"
```

> Types will be added here as the platform domain model is defined.
