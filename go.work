go 1.25.0

use (
	// Platform control plane API (modular monolith)
	.

	// Shared Go library
	./packages

	// Plugin contract SDK (local dev; published to github.com/kleffio/plugin-sdk for independent builds)
	../plugin-sdk
)
